package USML.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class SmallStepConsistencyEnum_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<SmallStepConsistencyEnum> constants = ListSequence.fromList(SmallStepConsistencyEnum.getConstants()).iterator();
    while (constants.hasNext()) {
      SmallStepConsistencyEnum constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<SmallStepConsistencyEnum> constants = ListSequence.fromList(SmallStepConsistencyEnum.getConstants()).iterator();
    while (constants.hasNext()) {
      SmallStepConsistencyEnum constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    SmallStepConsistencyEnum constant = SmallStepConsistencyEnum.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
