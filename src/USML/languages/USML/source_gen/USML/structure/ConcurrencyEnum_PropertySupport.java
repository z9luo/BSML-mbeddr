package USML.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class ConcurrencyEnum_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<ConcurrencyEnum> constants = ListSequence.fromList(ConcurrencyEnum.getConstants()).iterator();
    while (constants.hasNext()) {
      ConcurrencyEnum constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<ConcurrencyEnum> constants = ListSequence.fromList(ConcurrencyEnum.getConstants()).iterator();
    while (constants.hasNext()) {
      ConcurrencyEnum constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    ConcurrencyEnum constant = ConcurrencyEnum.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
