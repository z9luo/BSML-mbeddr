package USML.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class PreemptionEnum_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<PreemptionEnum> constants = ListSequence.fromList(PreemptionEnum.getConstants()).iterator();
    while (constants.hasNext()) {
      PreemptionEnum constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<PreemptionEnum> constants = ListSequence.fromList(PreemptionEnum.getConstants()).iterator();
    while (constants.hasNext()) {
      PreemptionEnum constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    PreemptionEnum constant = PreemptionEnum.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
